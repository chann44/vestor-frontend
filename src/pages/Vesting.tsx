import { Layout } from "../componets/Layout"
import { MOdal } from "../componets/Vesting/MOdal"

export const Vesting = () => {
    return (
        <Layout>
        <div>
            <MOdal />
        </div>
        </Layout>
   )
}